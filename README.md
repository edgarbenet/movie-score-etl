# movie-score-etl
Implementing a data pipeline that ingests, cleans, standardizes, and combines movie data from three different providers. The goal is to create a unified, queryable dataset for a data science team.
